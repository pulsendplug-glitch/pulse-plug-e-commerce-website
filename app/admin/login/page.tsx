'use client'
import { FormEvent, useState } from 'react'
import { LockKeyhole } from 'lucide-react'
export default function AdminLogin(){const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');const res=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});const data=await res.json();setBusy(false);if(!res.ok)return setError(data.error||'Login failed');window.location.href='/admin'}return <section className="adminAuth"><div className="adminAuthCard"><div className="adminIcon"><LockKeyhole size={22}/></div><span className="eyebrow blue">PULSE & PLUG</span><h1>Product Manager</h1><p>Private catalog management. Add products and upload product photos without editing website code.</p><form onSubmit={submit}><label className="field"><span>Admin password</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoFocus required/></label>{error&&<p className="error">{error}</p>}<button className="btn primary fullBtn" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form></div></section>}
