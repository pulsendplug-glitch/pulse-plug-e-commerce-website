import Link from 'next/link';import {products} from '@/lib/products';
export const metadata={title:'Shop All Collections | Pulse & Plug',description:'Browse Advanced Recovery Systems, Targeted Therapy Devices, and Daily Wellness Essentials from Pulse & Plug.'};
export default function Shop(){const cats=['Advanced Recovery Systems','Targeted Therapy Devices','Daily Wellness Essentials'];return <><section className="pageHero"><div className="container"><span className="offer">20% off with code LAUNCH20</span><h1>The Collections</h1><p>Premium health, recovery, and wellness — organized into three curated tiers so you can find exactly what your body needs.</p></div></section><nav className="tabs"><div className="container">{cats.map(c=><a className="pill" href={'#'+c.toLowerCase().replaceAll(' ','-')} key={c}>{c}</a>)}</div></nav><div className="container">{cats.map(cat=><section className="category" id={cat.toLowerCase().replaceAll(' ','-')} key={cat}><div className="categoryTitle"><h2>{cat}</h2><p>{cat==='Advanced Recovery Systems'?'Flagship, high-ticket systems engineered for whole-body restoration, circulation, and deep therapeutic recovery.':cat==='Targeted Therapy Devices'?'Focused, professional-grade devices that relieve tension and pain in specific areas of the body.':'Premium consumables and accessories to support recovery, relaxation, and healthy daily rituals.'}</p></div><div className="products">{products.filter(p=>p.category===cat).map(p=><article className="card product" key={p.slug}><Link href={'/product/'+p.slug}><img src={p.image} alt={p.name}/></Link><div className="cardBody"><Link href={'/product/'+p.slug}><h3>{p.name}</h3></Link><p>{p.description}</p><div className="productRow"><span className="price">${p.price.toLocaleString()}</span><Link className="btn primary" href={'/product/'+p.slug}>Add</Link></div></div></article>)}</div></section>)}</div></>}
