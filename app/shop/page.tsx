import Link from 'next/link'
import { ArrowRight, PackageOpen } from 'lucide-react'
import { listProducts } from '@/lib/db'
export const dynamic='force-dynamic'
export default async function ShopPage(){let products:Awaited<ReturnType<typeof listProducts>>=[];try{products=await listProducts(false)}catch{}return <><section className="pageHero"><div className="container"><span className="eyebrow blue">PULSE & PLUG CATALOG</span><h1>Shop premium equipment.</h1><p>Your catalog is ready for you to populate. Products added in the admin dashboard automatically appear here.</p></div></section><section className="section"><div className="container">{products.length===0?<div className="empty"><PackageOpen size={34}/><h2>No products yet.</h2><p>Add your first product, price, category and photo from the private Product Manager.</p><Link className="btn primary" href="/admin">Go to Product Manager <ArrowRight size={16}/></Link></div>:<div className="products">{products.map(p=><article className="card product" key={p.id}><Link href={'/product/'+p.slug} className="productImageWrap"><img src={p.imageUrl} alt={p.name}/></Link><div className="cardBody"><span className="eyebrow blue">{p.category}</span><Link href={'/product/'+p.slug}><h3>{p.name}</h3></Link><p>{p.description}</p><div className="productRow"><span className="price">${p.price.toLocaleString()}</span><Link className="btn primary smallBtn" href={'/product/'+p.slug}>View <ArrowRight size={15}/></Link></div></div></article>)}</div>}</div></section></>}
