import {products} from '@/lib/products';
import Link from 'next/link';
import {notFound} from 'next/navigation';
import type {Metadata} from 'next';
import ProductViewer from '@/app/components/product-viewer';
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)return{title:'Product Not Found | Pulse & Plug'};return{title:`${p.name} | Pulse & Plug`,description:`Shop the ${p.name} from Pulse & Plug. ${p.description}`,alternates:{canonical:`/product/${p.slug}`},openGraph:{title:`${p.name} | Pulse & Plug`,description:p.description,type:'website',images:[{url:'/icon.svg',alt:'Pulse & Plug Relief & Power'}]}}}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)return notFound();const siteUrl='https://pulse-plug-e-commerce-website.vercel.app';const productJsonLd={'@context':'https://schema.org','@type':'Product',name:p.name,description:p.description,image:[`${siteUrl}/icon.svg`],sku:p.slug,brand:{'@type':'Brand',name:'Pulse & Plug'},offers:{'@type':'Offer',url:`${siteUrl}/product/${p.slug}`,priceCurrency:'USD',price:p.price.toFixed(2),availability:'https://schema.org/InStock',seller:{'@type':'Organization',name:'Pulse & Plug'}}};const breadcrumbJsonLd={'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:siteUrl},{'@type':'ListItem',position:2,name:'Shop',item:`${siteUrl}/shop`},{'@type':'ListItem',position:3,name:p.name,item:`${siteUrl}/product/${p.slug}`}]};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(productJsonLd)}}/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(breadcrumbJsonLd)}}/><div className="container productDetail"><div><ProductViewer name={p.name} image={p.image}/></div><div className="detail"><p className="blue"><b>{p.category}</b></p><h1>{p.name}</h1><p className="price">${p.price.toLocaleString()}</p><p className="lead">{p.description}</p><p className="muted">Premium equipment selected by Pulse &amp; Plug for quality, comfort and recovery. Free white glove delivery is available on Advanced Recovery Systems.</p><div className="buttons"><Link className="btn primary" href="/cart">Add to cart</Link><Link className="btn" href="/contact">Ask a specialist</Link></div><hr style={{border:0,borderTop:'1px solid #dbe2eb',margin:'30px 0'}}/><h3>Secure &amp; trusted purchase</h3><p className="muted">Encrypted checkout · Flexible payment options · Expert support</p>{p.businessPrice>0&&<p className="businessPrice"><b>Business pricing:</b> ${p.businessPrice.toLocaleString()}</p>}</div></div></>}
