import {products} from '@/lib/products';import Link from 'next/link';import {notFound} from 'next/navigation';
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)return notFound();return <div className="container productDetail"><div><img className="detailImg" src={p.image} alt={p.name}/></div><div className="detail"><p className="blue"><b>{p.category}</b></p><h1>{p.name}</h1><p className="price">${p.price.toLocaleString()}</p><p className="lead">{p.description}</p><p className="muted">Premium equipment selected by Pulse & Plug for quality, comfort and recovery. Free white-glove delivery is available on Advanced Recovery Systems.</p><div className="buttons"><Link className="btn primary" href="/cart">Add to cart</Link><Link className="btn" href="/contact">Ask a specialist</Link></div><hr style={{border:0,borderTop:'1px solid #dbe2eb',margin:'30px 0'}}/><h3>Secure & trusted purchase</h3><p className="muted">Encrypted checkout · Flexible payment options · Expert support</p></div></div>}
