import Link from 'next/link'
import { ArrowLeft, ArrowRight } from 'lucide-react'
import { getProductBySlug } from '@/lib/db'
export const dynamic='force-dynamic'
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const product=await getProductBySlug(slug);if(!product)return <section className="section"><div className="container empty"><h1>Product not found</h1><p>This product is not available in the catalog.</p><Link className="btn primary" href="/shop">Back to Shop <ArrowRight size={16}/></Link></div></section>;return <section className="container productDetail"><div><img className="detailImg" src={product.imageUrl} alt={product.name}/></div><div className="detail"><span className="eyebrow blue">{product.category}</span><h1>{product.name}</h1><div className="price">${product.price.toLocaleString()}</div><p className="lead">{product.description}</p>{product.businessPrice!=null&&<div className="businessPrice"><strong>Business price</strong><br/>${product.businessPrice.toLocaleString()}</div>}<div className="buttons"><button className="btn primary">Add to cart</button><Link className="btn" href="/shop"><ArrowLeft size={16}/> Back to shop</Link></div></div></section>}
