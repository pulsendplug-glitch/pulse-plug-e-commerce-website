import 'use client'
import Link from 'next/link'
import { Menu, Moon, ShoppingBag, Sun, X } from 'lucide-react'
import { useEffect, useState } from 'react'
export default function SiteHeader(){const[dark,setDark]=useState(false);const[open,setOpen]=useState(false);useEffect(()=>{const isDark=localStorage.getItem('pulse-theme')==='dark';setDark(isDark);document.documentElement.classList.toggle('dark',isDark)},[]);function toggleTheme(){const next=!dark;setDark(next);document.documentElement.classList.toggle('dark',next);localStorage.setItem('pulse-theme',next?'dark':'light')}return <header className="siteHeader"><div className="topbar"><div className="container topbarInner"><span>Free white-glove delivery on Advanced Recovery Systems</span><div className="toplinks"><a href="tel:+17864066937">+1 786 406 6937</a><a href="mailto:pulsendplug@gmail.com">pulsendplug@gmail.com</a></div></div></div><div className="header"><div className="container nav"><Link href="/" className="brand" aria-label="Pulse & Plug home"><img src="/icon.svg" alt="" className="logoMark"/><span><strong>PULSE & PLUG</strong><small>Relief & Power</small></span></Link><nav className="links"><Link href="/">Home</Link><Link href="/shop">Shop</Link><Link href="/contact">Contact</Link></nav><div className="actions"><button className="iconbtn" onClick={toggleTheme} aria-label="Toggle theme">{dark?<Sun size={19}/>:<Moon size={19}/>}</button><Link className="iconbtn" href="/shop" aria-label="Shopping bag"><ShoppingBag size={20}/></Link><button className="iconbtn mobile" onClick={()=>setOpen(v=>!v)} aria-label="Open menu">{open?<X size={21}/>:<Menu size={21}/>}</button></div></div>{open&&<div className="mobileMenu container"><Link href="/" onClick={()=>setOpen(false)}>Home</Link><Link href="/shop" onClick={()=>setOpen(false)}>Shop</Link><Link href="/contact" onClick={()=>setOpen(false)}>Contact</Link></div>}</div></header>}
